__pycache__
.ipynb_checkpoints
.Trash-0
.env
.venv
# Local-only uv override (e.g. to drop the exclude-newer pin on hosts whose
# package index does not expose upload timestamps). Kept out of git so the
# committed exclude-newer pin still applies in CI / clean checkouts.
uv.toml
.*cache
**/*task.json
**/*run.json
dev/
_site/
site_libs/
docs/
.kagent-context/**
.claude/settings.local.json
# Auto-generated by the devcontainer CLI (pins feature versions); regenerated
# on build, so keep it local.
.devcontainer/devcontainer-lock.json
